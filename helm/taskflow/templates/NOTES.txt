TaskFlow has been deployed!

To access the application:
  1. Add taskflow.local to your hosts file:
       Windows: C:\Windows\System32\drivers\etc\hosts
       Add: 127.0.0.1 taskflow.local

  2. Open your browser: http://taskflow.local

Deployed services:
  - frontend      ({{ .Values.frontend.replicaCount }} replica(s))
  - api-gateway   ({{ index .Values "api-gateway" "replicaCount" }} replica(s))
  - user-service  ({{ index .Values "user-service" "replicaCount" }} replica(s))
  - task-service  ({{ index .Values "task-service" "replicaCount" }} replica(s))
  - postgres      (1 replica)

Useful commands:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get ingressroutes -n {{ .Release.Namespace }}
  helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
